"use client";

// The bill upload feature has been removed entirely from the system.
// This component is retained as an empty shell returning null to maintain build stability.
export function BillUploadModal() {
  return null;
}
